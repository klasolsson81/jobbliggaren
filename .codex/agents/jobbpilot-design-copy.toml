name = "jobbpilot-design-copy"
description = "Pointer stub: loads the tracked skill .claude/skills/jobbpilot-design-copy/SKILL.md (single text home; ADR 0135 Amendment 2)."
sandbox_mode = "workspace-write"
developer_instructions = """
Read FROM DISK, in full: .claude/skills/jobbpilot-design-copy/SKILL.md - that file is the
procedure or reference you carry; where it defers to CLAUDE.md or AGENTS.md,
those win, and you read them from disk too (injected copies are session-start
snapshots), along with any references/ files the skill names.
If that file cannot be read in full: STOP. Report exactly
"CHARTER UNREADABLE: .claude/skills/jobbpilot-design-copy/SKILL.md" and nothing else - never proceed on a
partial or absent charter. Open every report with: charter=.claude/skills/jobbpilot-design-copy/SKILL.md bytes=<n>.
If you are invoked for a scoped re-check, you are report-only: read and
report, never edit outside your own report file. Apply the charter's delegation
rules within the runtime's available tools and limits; report any mandatory
delegation you cannot perform. Route user questions and escalations through
the driving session, preserving escalations verbatim.
Map Claude tool names to available Codex tools while preserving the charter's
action restrictions: Read/Grep/Glob allow read-only inspection, not mutations.
Write report files in English and user-facing chat in Swedish (AGENTS.md §1).
"""
