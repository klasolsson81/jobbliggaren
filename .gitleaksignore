# gitleaks false-positive allowlist (pre-push hook: full-history scan over the SHARED
# object database - findings on sibling worktree branches block every session's push).
#
# The 'NyttL0senord123456' test-fixture false positive that the #688 session temporarily
# fingerprinted here (auth-lane branch feat/change-password-678) now has its durable fix: an
# inline `gitleaks:allow` on the declaration in ChangePasswordTests.cs. That is stable across
# rebase + squash-merge, whereas a commit fingerprint here re-breaks on every re-SHA. Prefer an
# inline allow (or a fixture rename) over a fingerprint for in-flight branch content.
