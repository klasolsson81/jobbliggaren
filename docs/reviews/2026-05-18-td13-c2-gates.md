# TD-13 C2-gates — dotnet-architect (seam-ruling) + security-auditor (BLOCKING)

**Datum:** 2026-05-18
**Batch:** C2 — per-användare-DEK-store + scoped cache + crypto-erasure-port + migration
**On-disk vid review:** `1162f1c` (hotfix) + C2 uncommittat
**Tester:** unit 485/485 · arch 59/59 (3 nya FieldEncryptionKeyStore) · Worker-integ 35/35 (9 nya C2-scenarier) · Api-integ 344/344

## dotnet-architect (agentId a7ce0c4e0fa758c88) — pre-code seam-ruling, GO

3 seams avgjorda entydigt (ingen CTO-flagg):
- **Seam 1 (Block→löst):** KMS-test-seam Variant A — WorkerTestFixture default-fake-KMS + per-scope. Produktkod orörd.
- **Seam 2 (Major→bekräftad):** `IUserDataKeyStore.DeleteDataKeysAsync(JobSeekerId, ct)` — C6 anropar inom AccountHardDeleter-transaktion; ingen UserDataKey-typläcka; idempotent.
- **Seam 3 (Minor→löst):** introspektion `internal` på `ScopedUserDataKeyCache` + `[InternalsVisibleTo]`, ej på porten.
Kontrakts-ytan bekräftad; CC-direkt-implementerad rakt av.

## security-auditor (agentId ad13917c17b378d71) — GO (villkorat, veto EJ utlöst)

| Severity | Antal |
|---|---|
| Critical | 0 |
| High | 0 |
| GDPR | 0 |
| Medium | 0 |
| Low | 2 (defense-in-depth, ej blockerande, ej TD-värda §9.6) |

7 auditpunkter alla gröna:
1. Wrapped-DEK at rest — endast CMK-wrappad persisteras, aldrig PlaintextDek (scenario 4 bevisar).
2. **Cache-zeroing (C1 Minor 2-överlämning, KRITISK) — STÄNGD:** Dispose ZeroMemory:ar alla buffrar, caller får Clone-kopia, ingen AsyncLocal/static, scenario 8 bevisar empiriskt.
3. Crypto-erasure — alla versioner, ExecuteDeleteAsync i ambient-transaktion (C6-atomicitet tekniskt uppfyllbar via delad scoped AppDbContext), idempotent; FK ON DELETE CASCADE = korrekt orphan-defense.
4. Cross-user-isolering — trippelskydd: PK (job_seeker_id,dek_version) + KMS encryption-context owner-AAD + value-equality cache-nyckel.
5. §5.4 — noll loggsatser i store/cache; validator loggar bara env-namn (ej CmkKeyId-värde).
6. IAppDbContext-isolering (FRÅGA 2) — UserDataKey onåbar från Application; arch-test kodifierar.
7. Migration — korrekta typer, FK-råsql korrekt, icke-destruktiv, down() säker, ingen PII.

**Low 1:** C6-integration utestående (korrekt fas-avgränsning, ej C2-fel). **Obligatorisk C6-gate-punkt:** verifiera att `AccountHardDeleter.HardDeleteAccountAsync` anropar `DeleteDataKeysAsync` *inom* transaktionen före `CommitAsync` (annars bryts Art. 17-atomicitet).
**Low 2:** `wrapped_dek` saknar längd-validering vid materialisering — defense-in-depth, KMS-runtime-guard är faktiskt skydd; valfri in-block-härdning, ej TD.

## Slutsats

Båda gates GO. Inga Critical/High/GDPR/Medium — veto ej utlöst, ingen Klas-eskalering. C1 security Minor 2 formellt stängd + empiriskt bevisad (scenario 8). Inga in-block-fix-krav. Hotfix Approach D (`1162f1c`) verifierad ren i C2-kontext (validator loggar ej nyckelmaterial). **Överlämnat till C6-gaten:** AccountHardDeleter→DeleteDataKeysAsync inom transaktion (Art. 17-atomicitet).
