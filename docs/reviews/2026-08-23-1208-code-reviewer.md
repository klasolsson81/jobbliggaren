# Code-review — #1208, dev-sinkens body-invariant flyttad till sin writer

**Agent:** code-reviewer (CLAUDE.md §9.2) · **Datum:** 2026-08-23
**Bas:** `75dbba78` · **Head:** `ec91a7c0` · **Branch:** `fix/dev-seq-pii-1208` · **PR:** #1474

## Code-review: Infrastructure/Email + spec/runbook-prosa (PR #1474)

**Status:** ⚠ Changes requested
**Auktoritet:** AGENTS.md §5 (`Comments:`, `Tests:`), §2.1, §3, §7 · CLAUDE.md §9.6, §11, §12
**Scope:** Infrastructure (Email, Logging, DI-doc) · Application.UnitTests · CLAUDE.md §11 ·
docker-compose.yml · .env.example · local-dev-setup.md. Ingen FE-yta i PR:ens egen scope.

⚠ **Scope-korrigering.** `git diff 75dbba78...HEAD` utesluter INTE de sjutton web-filerna:
`75dbba78` är en ancestor till `ec91a7c0` (via base-mergen), så tre punkter degenererar till
två här. Granskat delta är `git diff 80fef6e7^ fcc30e2b` = **8 filer**. De sjutton web-filerna
tillhör #1472/#1468 och är inte granskade.

### Blockers

Inga.

### Major

1. **Svepet missade tre live-ytor som fortfarande lovar det gamla, obetingade beteendet** —
   Fil: `src/Jobbliggaren.Infrastructure/Email/NullEmailSender.cs:10-13` ·
   `src/Jobbliggaren.Infrastructure/DependencyInjection.cs:999-1000` ·
   `src/Jobbliggaren.Api/appsettings.Local.json.example:44`
   Nuvarande: alla tre påstår i presens och utan villkor vad `ConsoleEmailSender` skriver —
   *"writes the recipient email + notification body to `ILogger` … so it must never run in a
   sink-backed, real-recipient environment"*, *"skriver mottagar-email + plaintext-token till
   ILogger"*, *"mail is written to the log, confirmation and activation links included"*.
   Efter grinden skrivs i en **real-recipient**-miljö varken mottagare eller kropp, alltså är
   premissen falsk i exakt det scenario meningarna resonerar om. Alla tre formulerar sig på
   **mottagar-/länk-axeln**, aldrig på kropps-axeln — därför nådde sessionens fyra mönster
   (`hela .\?PlainTextBody|hela mejlkroppen|whole body|entire body`) ingen av dem. Verifierat:
   kommandot kört ordagrant ger 11 träffar, noll i dessa filer.
   `NullEmailSender.cs` är dessutom **punkt 4 i CTO-bindets in-block-lista** och är oåtgärdad.
   `appsettings.Local.json.example` är den yta CLAUDE.md §11:s dev-boot-kontrakt parar med
   `local-dev-setup.md` — runbooken fick den nya paragrafen, mallen inte, så en utvecklare som
   följer mallen registrerar med sin riktiga adress och får ingen länk.
   Krävs: stryk den falsifierade premissen i de två C#-hemmen (slutsatsen bär på egen grund);
   i mallen räcker inte ren strykning — smalna meningen som §11 självt smalnades i denna PR.
   Motivering: AGENTS.md §5 `Comments:` — *"a factually wrong comment … is a defect and is
   fixed"*. §12 undantar `Comments:` från STOPP: merge-blockerande Major, **inte** §12-STOPP.
   Routas in-block (§9.6) — samma change-reason, aldrig ett issue.
   Delegera till: dotnet-architect

### Minor

2. **"a sink with no retention" läses naturligt som motsatsen till vad den ska betyda** —
   Fil: `src/Jobbliggaren.Infrastructure/Email/ConsoleEmailSender.cs:226` + `:243`
   Nuvarande: klausulen är **sakligt korrekt** — `75dbba78` (#1170) mätte 2026-08-23 med fyra
   instrument att ingen retention-policy finns — men engelskan läses först som "sänkan behåller
   inget", vilket i log-raden 3008 gör den egna motiveringen bakvänd. `(CLAUDE.md §11)` bär
   inte fakta: `grep -i retention CLAUDE.md` = 0 träffar; hemmet är #1170.
   Krävs: "no retention **limit**", eller stryk klausulen (§9.6 föredrar strykning).
   Delegera till: dotnet-architect

3. **"as a compile-time set" om en `FrozenSet`/`ImmutableArray` i static-initierare** —
   Fil: `CLAUDE.md:442`
   Nuvarande: båda initieras av static-konstruktorn, alltså vid runtime. Den operativa
   utsagan ("aldrig ett `IOptions`", kan inte vidgas i drift) är sann; ordvalet är ärvt från
   CTO-bindet. Krävs: "compile-time" → "fixed/konstant". Tio sekunder, annars namngiven skip.
   Delegera till: dotnet-architect

4. **Två integrationssviter som komponerar `ConsoleEmailSender` saknar `total:`-rad** —
   Fil: `tests/Jobbliggaren.Api.IntegrationTests/Configuration/AddEmailSenderGateTests.cs:205`
   Nuvarande: filen är ändrad men dess svit är inte körd; `WorkerTestFixture.cs:143` komponerar
   samma sender. Risken är **mätt nominell**: ändringen i Api-suiten är kommentar-only, och
   `FollowedCompanyDigestIntegrationTests` + `DigestDispatchJobIntegrationTests` innehåller noll
   `LogLevel`/`EventId`/`RecordingLogger`-assertions och noll adress-literaler — grinden kan
   inte nå något de påstår. Krävs: kör båda och redovisa `total:` (AGENTS.md §7 — beviset är
   `total:`-raden, aldrig exitkoden); Testcontainers är parallellsäkra (CLAUDE.md §6.5).
   Delegera till: sessionen (körning, inte testskrivning)

### Bra gjort

- `WriteEmail` är ett **mätt komplett snitt**: alla 8 `IEmailSender`-metoder går dit, `LogEmail`
  anropas från exakt ett ställe, EventId 3001 + template orörda, 3008 kolliderar inte (3001/3002/3005/3006/3007).
- Predikatet håller i **fail-open-riktningen**: varje `true`-gren landar på en IANA-hållen eller
  odelegerad etikett; label-boundary, FQDN-punkt, `LastIndexOf('@')` och `OrdinalIgnoreCase`-comparern
  är alla pinnade av namngivna fall.
- Testet namnger själv varför probens värde inte behöver se ut som en token — §5 `Tests:`-premissen
  är avhandlad i filen i stället för att lämnas åt granskaren.

### Sammanfattning

**0 Blocker, 1 Major, 3 Minor.** Major 1 fixas in-block (§9.6 — samma change-reason, aldrig ett
issue); Minor 2–3 är tio-sekunders-fixar eller namngivna skips i PR-bodyn; Minor 4 är en körning.
Delegering: dotnet-architect (1–3), sessionen (4). Mekanismen, predikatet, `internal`-seamen och
CanDeliver-paragrafen är godkända som de står. Re-review efter fix: samma agent, report-only,
scopad till fix-deltat (CLAUDE.md §9.6).
