# code-reviewer — PR #1466 (#1463)

> Transkriberad ordagrant av ägande session. Charter är read-only. Ingen eskalering (inga
> CLAUDE.md-konflikter).
>
> **Runda 1:** 0 Blockers · 1 Major · 5 Minor. Status ⚠ Changes requested.

**Granskat HEAD:** `dc6d967a` mot base `7b318483` (blob-läsning, ingen build/test körd)
**Auktoritet:** CLAUDE.md/AGENTS.md §2.1 · §3 · §5 (`Comments:`, `Tests:`) · §7 · §9.6 · §12
**Scope:** Infrastructure + Api.IntegrationTests + CTO-transkriptet. Områdena 1–6 kördes alla;
frontend saknas i diffen.

**Mekaniska renames — verifierat.** Nio `db.` → `_db.` plus en `tokenizer.` → `_tokenizer.`. Varje hunk
är ett rent `-`/`+`-par där enda skillnaden är understrecket; ingenting annat flyttade. Testfilen har
exakt **en** borttagen innehållsrad (`.UseSnakeCaseNamingConvention());` → samma rad plus
`.AddInterceptors(_commandTimeouts));`), resten är additivt.

**D1/D2/D3 är inte omprövade.** 180, halva taket och konstruktorplaceringen graderas som
*implementation*, inte som beslut.

## Blockers

Inga.

## Major

**1. Faktafel i kommentar — uttömmandepåståendet om EF-nivå-siter är falskt**
Fil: `src/Jobbliggaren.Infrastructure/JobAds/RecruiterErasureMatchQuery.cs:59-62`

Nuvarande: *"This is the repo's FIRST EF-level SetCommandTimeout on AppDbContext. The four existing
constants sit on raw NpgsqlCommands … So the absence of other EF-level sites is a fact, not a gap."*

Mätt: de tre första meningarna **håller**. `SetCommandTimeout` har noll andra hem i `src/`, och de fyra
konstanterna (`CompanyRegisterSearchQuery:63`, `CompanyWatchBrowseQuery:80`, `ScbCompanyRegisterStore:34`
+ `:35`) sitter alla på `connection.CreateCommand()`-kommandon — verifierat på varje anropsplats.

**Slutmeningen gör det inte.** `src/Jobbliggaren.Infrastructure/Persistence/MigrationsOptionsFactory.cs:35`
sätter `npgsql.CommandTimeout(600)` inne i `BuildAppOptions` — en **EF-nivå** command-timeout, på
`DbContextOptions<AppDbContext>`. Det är en annan EF-nivå-site på AppDbContext, och "the absence … is a
fact" är därmed osant som skrivet. Den snäva läsningen ("andra `SetCommandTimeout`-siter") räddar
meningen men står inte där, och nästa läsare som frågar *"finns det något annat EF-nivå-tak på
AppDbContext?"* får fel svar av den här filen.

Krävs: radera meningen (första halvan av stycket bär redan hela poängen), eller skriv ut avgränsningen
`SetCommandTimeout` + runtime.
Motivering: AGENTS.md §5 `Comments:` — *"A factually wrong comment … is a defect and is fixed."*
Charter: faktafel i kommentar = Major.
**Stängs mekaniskt** per §9.6 om fixen är ren radering (gammal sträng grepper till noll, noll `+`-rader
i filen). Ingen omkontroll skyldig då.

## Minor

Alla fem är §5 `Comments:` — **inte** §12-STOPP (§12 carve-out). Botten är strykning, inte omformulering.

**2. Kommentardensitet: samma kunskapsstycke i tre till fem hem, inom en och samma PR.**
Mätt: 110 av 163 tillagda rader i produktionsfilen är kommentarrader. Tre konkreta dubbleringar, var och
en med en **radbar** kopia: *Art. 12(2)/17(1)-argumentet* i fem hem; *CTO D2 och D3 återargumenteras
ordagrant i koden* medan rulingen ligger spårad i samma PR; *tautologi-argumentet* i två hem;
*5–6 s-mätningen* två gånger i samma fil. Behåll ett hem per kunskapsstycke; radera kopiorna.

**3. "Regenerate"-kommandot regenererar ingenting.** Kört ordagrant ger strängen en interaktiv
psql-prompt, inte talen. §5 kräver *"publish the command that regenerates it"*. Samma stycke gör en
självmotsägelse: *"do not trust the numbers"* förklarar dem **levande** (och då förfaller de), medan
datummärkningen gör dem till daterad provenance. Dessutom: `statement_timeout is 0` är en levande
mätning **utan eget datum** — `read 2026-08-23` hänger på Npgsql-doc-URL:en, inte på servermätningen.
Välj en form.

**4. Konstruktorns `<summary>` säger det de tre nästa raderna visar.** §5: *"prose restating what the
next line already shows"*. Radera.

**5. "TWO commands" är villkorligt falskt.** På noll-träff-vägen returnerar metoden innan
projektionen — då utfärdas **ett** kommando, och det är exakt den väg testet kör. Bärande delen av
argumentet överlever, talet gör det inte. "up to two", eller radera parentesen.

**6. PR-bodyns renameräkning stämmer inte med diffen.** *"the whole of the 10 mechanical `db.`→`_db.`
renames"* — mätt: **nio** `db.`→`_db.` plus **en** `tokenizer.`→`_tokenizer.`.

## Bra gjort

- **Pinnen korsar egenskapen.** `CommandTimeoutRecorder` läser `CommandTimeout` av det verkliga
  `DbCommand`-objektet, och kontrollhalvan är **genuint icke-vakuös** — en färsk `CreateScope()` ger en
  AppDbContext som aldrig fått `SetCommandTimeout`, och Testcontainers connection string bär inget
  `Command Timeout`, så 30 är Npgsqls egen default. Den stänger också det led `dotnet-architect` skrev
  som omätt. `ShouldNotBeEmpty` på båda halvorna gör vakuum omöjligt.
- **PII-vägen är stängd och pinnad.** `LogMarginConsumed` bär `ElapsedMs` + `CeilingSeconds` och inget
  mer, och testet asserterar **fältuppsättningen**, inte den renderade strängen. `[LoggerMessage]`-state
  implementerar `IReadOnlyList<KeyValuePair<string, object?>>`, vilket den mätta mutationen bevisar.
  EventId 8436 kolliderar inte och fortsätter erasure-handlerns 8430–8435.
- **§2.1 rent, och det är den intressanta halvan.** `IAppDbContext` orörd, ingen `.csproj` i diffen,
  ingen provider- eller relational-referens i Application. `SetCommandTimeout` är relational-only —
  precis §2.1 axel 3:s member-fälla — och ligger på **rätt** sida av providergränsen.
  `Stopwatch.GetTimestamp`/`GetElapsedTime` rör inte §5:s `DateTime`-förbud. Och
  `RecruiterErasureTestServices` svalde den tredje konstruktorparametern utan en enda fixture-edit — det
  enda hemmet #1435 byggde höll vid första prövningen.

## Sammanfattning

0 blockers, 1 major, 5 minor. §5 `Tests:` rent: varningens premiss är ett tillstånd `src/` producerar
via klockan, och takpinnens premiss produceras helt av produktionsvägen. Inget §12-STOPP-fynd.

Major #1 är merge-blockerande (§6, §12) och fixas in-block — aldrig som issue. Är fixen ren radering
**stängs den mekaniskt** per §9.6. Om något av de fem minor-fynden i stället stängs med ny prosa är det
en ny granskningsbar claim och kostar en rond — radera i stället.

Re-review efter fix: samma agent, report-only, scopad till fix-deltat (CLAUDE.md §9.6).

---

## Sessionens åtgärd

**Major 1 — STÄNGD genom ren strykning.** Hela påståendet borta. `dotnet-architect` fällde samma stycke
oberoende på en **annan** mätning (tre `SetCommandTimeout(300)` i testfixturer), så strykningen stänger
båda fynden med en åtgärd.

**Minor 2 — STÄNGD.** Kopiorna raderade: Art. 12(2)-argumentet står nu i ett hem (konstanten),
CTO-argumenten är borta ur koden, tautologi-meningen står bara vid testet, och 5–6 s-talet bara vid
konstanten.

**Minor 3 — STÄNGD genom att välja en form.** "Regenerate"-stycket är raderat; talen är märkta som
daterad provenance för valet av 180, inte som ett levande påstående, och `statement_timeout`-mätningen
har fått sitt eget datum och sin egen miljö utskriven ("the dev server's … measured … the same day").

**Minor 4 — STÄNGD genom radering.**

**Minor 5 — STÄNGD.** "TWO commands" → "can issue a second one (the EF projection below, on a non-empty
match)".

**Minor 6 — STÄNGD** i PR-bodyns enda tillåtna edit: 9 `db.`→`_db.` plus 1
`tokenizer.`→`_tokenizer.`.
