# 2026-09-08 — #1706 slug + copy + breddgrindens docblocks + #1707 N=1 — code-reviewer

> ⚠ **Transkriberad av sessionen ur agentens svar** (hans charter ger `Read`/`Grep`/`Glob`).

**Status (första ronden):** ⚠ Changes requested — **0 Blockers, 3 Major, 3 Minor.**
**Auktoritet:** CLAUDE.md §9.2 · §9.6 · AGENTS.md §5 `Comments:` · §5 `Tests:` · §7 · §2.4 · §12
**Scope:** FE (Next.js/RSC + vitest), BE-docblocks, Architecture-testregister, `messages/{sv,en}`.
Ingen produktionslogik i BE ändrad; kontrollerat att inget paket eller lager rörts — rent.

## Major

**1. `anyTooBroad` förlorade sin ENDA pin — testet som ska bevaka den är vakuöst efter fixen**
Fil: `criteria-summary.test.tsx:276-285`. Vid N=1 är `.jp-appsummary__advice` 0 **oavsett**
`anyTooBroad`, eftersom `adviceStatedByCaller` är falsk. Mutation: stryk `anyTooBroad &&` →
sviten förblir grön. En muterad grind skulle skriva rådet under ett block där ingen rad vägras — **ett
falskt påstående till användaren.** *Före diffen pinnade testet grinden; diffen tog bort pinnen.*
Krävs: höj fixturen till **två** räknade kriterier och kör om mutationen. Samma rond:
`standalone`-flaggans N≥2-arm är nu också opinnad — en `dessa företag`-assertion i N≥2-testet stänger
båda. Delegera till `test-writer`.

**2. Copy-renamet gjorde tolv backend-docblocks faktiskt felaktiga: de citerar en renderad sträng som
inte längre finns.** `ICompanyWatchBrowseQuery.cs:370,375,407` · `CriterionMatchingAdSetResolver.cs:286,304,374`
· `ListCompanyWatchCriteriaQuery.cs:29` · `ICompanyWatchCriterionMaterialiser.cs:116` ·
`CompanyWatchCriterionMaterialisation.cs:24,107` · `CompanyWatchCriterionMaterialiser.cs:391` ·
`CompanyWatchCriterionMember.cs:52`. **Diffen skapade felaktigheten.**
Krävs: **stryk den citerade literalen.** Klistra INTE in den nya meningen — det vore ett andra hem för
copy, exakt det fix 3 nyss stängde i samma fil.
⚠ **Undantag som INTE ska svepas:** `criteria-summary.test.tsx:202-203` citerar de gamla strängarna
som **daterad provenance** för #1707-defekten.

**3. Två kommentarer i `criteria-summary.test.tsx` falsifierades av den fix de beskriver** — `:194`
förklarar en blocknivårad som inte längre renderas vid N=1; `:378-379` påstår att båda armarna
renderar samma korta status i ett test där `shortRefusal` är falsk. Krävs: stryk (a) helt, stryk
första satsen i (b).

## Minor

**4.** `A industry watch` × 3 i `messages/en/` — mekanisk substitution utan artikelbyte. `en` är en
stödd locale, alltså synlig copy. Krävs: `An industry watch`.
**5.** Kvarvarande `smartaBevakningar`-identifierare — inget är trasigt, `foretag-subnav.tsx:17-19`
dokumenterar glappet ärligt. Rekommenderad disposition: **namngiven skip i PR-kroppen**.
**6.** Ogrammatisk svenska efter substitutionen: *"de branschbevakningarnas rader"* (dubbelbestämning).

## Bra gjort
- Flaggdelningen är ren: `standalone` = ytan (2 ställen), `shortRefusal` = vägran-copyn (3 ställen),
  effektivt värde identiskt med `design-reviewer`s formel; båda anropsställena passerar ett värde
  beräknat **en** gång, och `dessa företag`-pinnen fångar den flipp hon förbjöd.
- Slug-flytten är komplett: `git grep smarta-bevakningar` = 0 utanför daterade review-rapporter.
- Testpremisserna håller §5 `Tests:` — fixturkommentaren namnger producenten.

---

## SKOPAD OMKONTROLL (rapport-läge) — fix-deltat `878e1fed..5264752e`

**Status:** ⚠ Changes requested — **0 Blockers, 1 Major, 1 Minor.** Båda stänger i **en enda ren
raderings-diff**, mekaniskt per §9.6 — ingen ytterligare omkontroll skyldig, ingen ny prosa.

**Major 1. Docblocket påstår att ett ankare ÖVERLEVDE en ommätning som samma delta nedgraderar** —
`CompanyWatchCriterionMember.cs:66-69`: *"The anchor that survives re-measurement is the budget at the
criterion cap"*. Mätt mot tre hem: rapportens x20-kolumn **är** budgetankaret och rapporten skriver
själv **"The x20 column is therefore a shape, not a verdict"** (9,9 % → **24,4 %** av budgeten); plan-
caveaten på `:132-137` undantar inte x20-kolumnen; och ADR 0139-amendmenten säger *"Kostnadsankaret
går inte att ta om rent"*. **Om ett kostnadsankare fortfarande bar 1 000 vore P1–P5 överflödigt.**
Krävs: radera hela `<para>`:t `:65-70`. **Ersätt inte** — varje ersättningsmening är ett nytt påstående
om ett tal vars status ADR 0139 lämnar till nästa PR.

**Minor 2. Ny exklusivitetsclaim pekar in i ett gitignorerat hem** — `:76`: *"Its status is settled in
that amendment and nowhere else"*, medan den **tracked** `2026-09-08-1706-form-cto.md:83-84` avgör
samma fråga. Krävs: radera meningen. Parentesen "(ADR 0139 Amendment 2026-09-08)" är provenance och
står kvar.

### Bra gjort (omkontrollen)
- Major 1–3 stängda och verifierade: `anyTooBroad`-pinnen är nu icke-vakuös och *"nothing else pins
  it"* håller; `"för bred"` grepar till noll som nutida påstående, och **de tre överlevarna är korrekt
  lämnade** — logg-templaten `:440`, sveptestets svenska prosa, och `criteria-summary.test.tsx:201-202`
  som är daterad + attribuerad historik, alltså §1.6-provenance som inte får svepas.
- Rapportens nya aritmetik räknad om post för post: alla fyra cellerna stämmer; `SweepBatchSize = 50`
  och `SweepCron = "* * * * *"` verifierade i koden; 1,6x-formen och den namngivna
  pristine-motsägelsen korrekta.
- Bucket-kopplingen navigerbar från båda hållen med `<c>`, **inte** `<see cref>` — Application får
  ingen kompileringsberoende till Api (§2.1 ren).

### Observation, INTE ett fynd (§9.6-skopning — orörd fil)
Deltat etablerar att *"A cap move re-derives `SweepBatchSize`"* och la symmetriskt in
`MaxPerCriterion` i rate-limit-hinkens recompute-trigger — men `CompanyWatchMaterialisationOptions.cs:126`
nycklar fortfarande sin trigger enbart på `SweepCron`. Värt en issue eller en namngiven skip.
