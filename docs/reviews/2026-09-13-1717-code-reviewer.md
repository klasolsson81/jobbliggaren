# 2026-09-13 — #1717 / PR #1722 — code-reviewer

> ⚠ **Transkriberad av sessionen ur agentens svar.** Innehållet är hans. Kapad till hans charters
> Output format (§9.2): max 25 rader per fynd, `Bra gjort` ≤ 3 rader, inget Q&A- eller
> metodavsnitt — rapporten är fyndlistan, och PR-kroppen bär verdict-raden, aldrig rapporten.

## Code-review: `/oversikt` radgrammatik — blockrubriker i Företagsbevakning (PR #1722, issue #1717)

**Status:** ⚠ Changes requested
**Auktoritet:** CLAUDE.md §§5 (`Comments:`, `Tests:`) · §4 · §7 · §9.6 · §12 · AGENTS.md §1
**Scope:** Frontend only. Ingen backend, inga migrationer, inga nya beroenden → §§2.1–2.3 ej
tillämpliga och ej granskade.
**Egna mätningar:** `pnpm exec vitest run src/components/oversikt` → `Test Files 12 passed (12)` /
`Tests 151 passed (151)` (parsebar summeringsrad, inte exit-kod). `pnpm tsc --noEmit` → exit 0.

### Blockers

Inga.

### Major

**1. Kommentarens radpekare namnger fel prop** — Fil: `company-summary.test.tsx:527`

Nuvarande: `` // gäst-demon skickar `null` (`guest-oversikt-page.tsx:271`) ``. Mätt vid HEAD
`44806e0d`: `guest-oversikt-page.tsx:271` är `linkHref={null}`. Det `heading={null}` meningen
påstår ligger på **:272**.

Krävs: pekaren rättas — eller hellre ersätts av något som inte förfaller:
`` guest-oversikt-page.tsx, `<CompanySummary>` `` grepar och överlever nästa redigering.

Motivering: CLAUDE.md §5 `Comments:` — *"A factually wrong comment — wrong number, wrong gate name,
stale §-reference — is a defect and is fixed."* Det är inte en formuleringsfråga: raden pekaren
träffar bär **också** `null`, så en läsare som verifierar påståendet ser `null` och bekräftar något
falskt. Det är just den felklassen som skiljer fel siffra från slarvig formulering.

### Minor

**1. Fyra kvarvarande levande radpekare i tracked filer** — `application-summary.test.tsx:54-55`,
`company-summary.test.tsx:526`, `criteria-summary.test.tsx:91`. Alla fyra korrekta i dag (mätt).
Klassen är namngiven i §5 `Comments:` som *"a live measured number in a tracked file — it decays
within a commit or two"*. Förfallotakten ligger i eget material: designrapporten skrevs mot commiten
före denna och bär redan `oversikt-page.tsx:455/464` för anropsställen som nu står på 464/474. **En
commit.** Fem pekare infördes, en var fel innan PR:en ens mergats. Icke-förfallande form: namnge
komponent/prop i stället för rad.

**2. `heading` skuggas inom samma fil** — `criteria-summary.tsx:51/97` mot `:190`. Propen (*blockets
namn*) och `const heading` inne i `items.map` (*en rads visningsetikett*) är två olika saker med
samma namn; filens egna kommentarer använder ordet i en tredje bemärkelse. Krävs: döp om **det
lokala** (`rowName`) — propen är den nya och externt synliga. Ingen bugg i dag (propen konsumeras
före `map`:en), men inget fångar en framtida: repots ESLint har ingen `no-shadow`, så `pnpm lint`
passerar skuggningen tyst.

**3. Docblocket importerar ett skäl som inte gäller komponenten** — `criteria-summary.tsx:42-44`.
Nuvarande: *"...the one surface that must not have one would get it without anyone choosing it."*
Mätt: `CriteriaSummary` har **ett** anropsställe och det skickar alltid rubriken. Samma PR säger
motsatsen uttryckligen i `criteria-summary.test.tsx:580-582`. Graderas **inte** Major: inlånat
resonemang, inte felaktig siffra/grindnamn/§-referens — men det lägger ett falskt spår.

**4. Wiring-testet transkriberar copy som deltat självt förbjuder** — `oversikt-page.test.tsx:640`.
Samma delta etablerar den motsatta idiomen två filer bort och skriver ut skälet
(`company-summary.test.tsx:40-42`). Krävs: läs `messages.oversikt.*.heading`. Ordningen pinnas
fortfarande, så en nyckelväxling faller.

**5. `style={{ margin: 0 }}` är en MÄTT no-op** — tre filer. Mätt i tre led: `preflight.css:7-16`
sätter `margin: 0` på `*` (v4, aktiv via `@import "tailwindcss"` på `globals.css:1`); `globals.css`
har **ingen** bar `p`-regel och **ingen** `.jp-appsummary p`-descendant; ingen typography-plugin i
`package.json`. **Formen är inte min att gradera och jag graderar den inte** — den är
`design-reviewer`s bindning ordagrant. Jag rapporterar bara mätningen: deklarationen renderar noll.
Delegera till `design-reviewer`, gå **inte** förbi henne.

### Bra gjort

- Mutationsfyndet är exakt den fälla som annars passerar: seamen datat passerar är muterad.
- §5 `Tests:` tillämpad rätt åt det svåra hållet — ingen fixtur bygger ett oproducerbart tillstånd.
- `--unavailable`-byte-identiteten är **pinnad** i två test i stället för påstådd.

### Sammanfattning

**1 Major, 5 Minor, 0 Blockers.** Majorn är merge-blockerande (§6, §12) och fixas in-block. Minor 1–4
är tio-sekundersredigeringar i filer deltat redan äger; §9.6:s cap talar för in-block framför filning.
Minor 5 routas till `design-reviewer`, inte till backloggen.

**Dina tre frågor, avgjorda (§9.6 "fyndet håller inte"):** `heading` på `ApplicationSummary` — inget
fynd, den är **bunden** av B1/B4 och inte vald av sessionen; residualen namnges utan gradering.
Grenduplicering — inget fynd; tre par à fyra rader i filer som annars inte delar något.
**CLAUDE.md-konflikter att bära till Klas: inga.** Varje agent-citat i deltat mätt mot rapporten;
enda felaktiga siffran är Major 1.

---

## SKOPAD OMKONTROLL (rapport-läge) — fix-deltat `44806e0d..d7a40753`

**Status:** ⚠ Changes requested — **1 ny Major i deltat**, alla sex tidigare fynd stängda
**Egna mätningar (ärvde inget):** `Test Files 12 passed (12)` / `Tests 151 passed (151)`; `tsc` exit 0.

### Stängda fynd

| # | Fynd | Verifiering | Dom |
|---|---|---|---|
| Major 1 | Fel radpekare | `company-summary.test.tsx:528-529` namnger nu elementen, noll siffror | **STÄNGD** |
| Minor 1 | Fyra levande radpekare | Egen grep: **alla fem PR:en införde är borta** | **STÄNGD** |
| Minor 2 | `heading`-skuggning | `criteria-summary.tsx:191` = `rowName`; grannkommentaren skiljer BLOCK från ROW | **STÄNGD** |
| Minor 3 | Inlånat ytpåstående | Meningen borta | **STÄNGD** (men se Major nedan) |
| Minor 4 | Transkriberade literaler | `oversikt-page.test.tsx:648-651` läser `messages.…` | **STÄNGD** |
| Minor 5 | `margin: 0`-no-op | Alla tre borta, **ingen** kompenserande CSS-regel; `globals.css`-deltat är 23+/23− och teckenidentiskt | **STÄNGD** |

⚠ **Skopanot, inget fynd:** två kvarvarande radpekare i `guest-oversikt-page.tsx:105` och `:138` finns
i basen `f987c62d` — utanför båda deltan, inte mina att resa här (§9.6). Nämns så att grepen inte läses
som globalt ren.

### Ny Major i deltat

**1. Fixen för Minor 3 införde ett nytt fel tal — samma klass som Major 1** — Fil:
`criteria-summary.tsx:44`. Nuvarande: *"The `null` arm exists so **the three summaries** share one
contract"*. Mätt vid `d7a40753`: `grep -rn "readonly heading" src/components/` → **två** bärare.
`application-summary.tsx` förlorade propen **i samma commit som skrev meningen**.

Krävs: satsen stryks. Motivering: §5 `Comments:` — fel tal är en defekt som lagas. Identisk felklass
som ursprungliga Major 1: meningen skrevs om utan att talet räknades om mot koden i samma commit.

⚠ **Stängningsform:** new-in-delta Major rest av en omkontroll → §9.6 anvisar **radering**. En radering
kan inte införa ett nytt påstående, stänger mekaniskt, **ingen ytterligare omkontroll skyldig**. Min
skopade omkontroll är förbrukad; stryk satsen, skriv inte om den.

### Ny Minor i deltat

**2. Vitrumsrad kvarlämnad där propen togs bort** — `application-summary.test.tsx:143`: sju blanksteg
och inget annat. `.editorconfig` `[*]` sätter `trim_trailing_whitespace = true`; `= false` gäller
enbart `[*.md]`. Mätt att inget fångar den: ingen Prettier (§11), ingen ESLint-regel — lint-körningen
passerade med artefakten på plats. Ren radering → stänger mekaniskt.

### Om `ApplicationSummary`-borttagningen

Godkänd, verifierad oberoende: `git diff f987c62d d7a40753 -- …/application-summary.tsx` ger **0
rader**. Jag har **inga invändningar mot att hon drog tillbaka sin egen föreskrift.** §9.6 lägger
severity och bindning hos den charter som rapporterade, och B1/B4 är hennes; att skilja *"renderar
byte-identiskt"* (kravet) från *"får `heading={null}`"* (mekaniken) är precis den distinktion bara hon
får göra. Deltat bär ingen §12-klass — CSS-deltat är en flytt, inte en tokenändring.

### Sammanfattning

**1 Major, 1 Minor, 0 Blockers — båda nya i fix-deltat, båda stängs genom ren radering.** Sex av sex
tidigare fynd stängda och verifierade med egna mätningar. Rundbudgeten intakt: radering stänger
mekaniskt, ingen ytterligare omkontroll skyldig, inget behöver gå till `blocked`. Efter de två
raderingarna: verifiera att HEAD är oförändrad omedelbart före `agents-done` (§9.6).
