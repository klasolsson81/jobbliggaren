# code-reviewer — PR #1725 (#1715 a+b, #1711), round 1

- **Date:** 2026-09-14
- **HEAD reviewed:** 804d9eff (base a2ef342e)
- **Mode:** report-only; transcribed verbatim by the driving session.

---

## Code-review: #1715 (a+b) + #1711 — frontend refusal copy + dialog count vocabulary (PR #1725)
**Status:** ⚠ Changes requested
**Auktoritet:** CLAUDE.md (AGENTS.md) §4, §5 `Tests:`, §5 `Comments:`, §7, §10, §12; CLAUDE.md §9.6
**Scope:** Frontend endast (`web/jobbliggaren-web`: 2 komponenter + 2 surfaces + 4 message-filer + 4 testfiler) samt en promotad CTO-rapport. Områdena 1–3 (Clean Architecture / DDD / CQRS) triggas inte — ingen `src/`-fil rörs; jag har läst `CriterionMatchingAdSetResolver` endast som mätningsunderlag. Granskade områden: 4 (tester), 5 (konventioner), 6 (anti-patterns, inkl. kommentarer). HEAD 804d9eff mot bas a2ef342e.

### Blockers
Inga.

### Major
1. **Faktiskt felaktig kommentar: gate-termen `|| i.matching.tooBroad` finns inte längre** — Fil: `web/jobbliggaren-web/src/components/company-criteria/criteria-section.test.tsx:214`
   Nuvarande: `// Without this arm the gate's second term (`|| i.matching.tooBroad`) could not be told apart from // its first in any run …`. Deltat (3050974f) ersatte just den gaten — `criteria-section.tsx:43-44` är nu `anyAdsTooBroad = items.some((i) => i.ads.tooBroad)` och `anyMatchingOnlyTooBroad = items.some((i) => i.matching.tooBroad && !i.ads.tooBroad)`. Greppat: `|| i.matching.tooBroad` förekommer noll gånger i `web/jobbliggaren-web/src`. Kommentaren namnger alltså en gate som deltat självt rev — exakt formen "när ett bind rivs faller de meningar som motiverade det", och CTO-rapporten svepte de tre motsvarande meningarna i `criteria-section.tsx`/`criteria-card.tsx` men inte denna.
   Krävs: den felaktiga termen bort. Testets *avsikt* står kvar och är korrekt — armen diskriminerar fortfarande, nu genom `anyMatchingOnlyTooBroad`.
   Motivering: CLAUDE.md §5 `Comments:` — "a factually wrong comment — wrong number, wrong gate name, stale §-reference — is a defect and is fixed"; min egen skala graderar det Major. Routing: in-block eller följd-PR, aldrig issue (§9.6).
   ⚠ Stängningsform: **radera parentesen** (`(`|| i.matching.tooBroad`)`) → strängen greppar till noll, stängande diff lägger noll rader, stängs mekaniskt utan omkontroll (§9.6). Skrivs termen i stället om till det nya namnet tillförs en rad och en scopad omkontroll hos mig återstår.
   Delegera till: test-writer.

### Minor
2. **`ads.tooBroadShort` står okvalificerad bredvid den nya `ads.matchingTooBroadShort`** — Fil: `messages/sv/pages.json:478` + `messages/en/pages.json:478` (konsumenter `criterion-ad-lines.tsx:213` och `:226`)
   Nuvarande: `tooBroadShort` (annons-armen) / `matchingTooBroadShort` (matchnings-armen). CTO-rapporten döpte om `tooBroadAdvice` → `adsTooBroadAdvice` med exakt motivet "an unqualified sibling name beside `matchingTooBroadAdvice` reads as the general case" — samma asymmetri uppstod i commit c1209391, som låg före domslutet, och rapporten uttalar sig inte om den. Ingen användarsynlig effekt.
   Krävs: `adsTooBroadShort` (2 message-filer + 2 anropsställen), eller en namngiven skip i PR-kroppen (§9.6).
   Motivering: CLAUDE.md §5 (magic strings/namngivning), §9.6 Minor-routing. Delegera till: nextjs-ui-engineer.

3. **Klasskopplad selektor i det nya dialogtestet** — Fil: `src/components/company-criteria/criterion-dialog.test.tsx:82-87`
   Nuvarande: `p.font-medium[aria-live="polite"]` + `.slice(0, 2)`. `font-medium` är en Tailwind-utility på `criterion-picker.tsx:256` — en ren stilklass, och en omstyling till t.ex. `font-semibold` bryter testet. Det **faller högt, inte tyst** (`counters()` → `[]` och `toEqual(["1 val","1 val"])` failar), vilket är varför detta är Minor och inte Major; `.slice(0, 2)` är däremot positionell och tyst om pickern någon gång renderar en tredje sådan rad.
   Krävs (förslag): ankra i semantiken som redan finns — `criterion-picker.tsx:181` renderar `role="group"` med `aria-label={groupAria}` ("Branscher"/"Kommuner"), så `within(screen.getByRole("group", { name: "Branscher" }))` ger axeln explicit och gör `.slice` onödig.
   Motivering: CLAUDE.md §7 (stabila tester), §4. Delegera till: test-writer.

4. **Kosmetik i `criteria-card.test.tsx`** — Fil: `src/components/oversikt/criteria-card.test.tsx:219-220` (saknad blankrad mellan det nya testet och `it("criteriaCardIsWide …")`, medan varje annat syskon i filen har en) och `:196-197` (ASCII-bindestreck `- a refused ad count -` där filen i övrigt använder tankstreck). Krävs: blankrad + tankstreck, eller namngiven skip. Motivering: CLAUDE.md §5 `Comments:` (frasering = Minor), §11 (`dotnet format`/ESLint fångar ingetdera). Delegera till: nextjs-ui-engineer.

### Bra gjort
- **Byte-identiteten är faktiskt pinnad, inte påstådd:** `criteria-section.test.tsx:195-198` asserterar hela `.jp-criteria-advice`-strängen för ads-armen, och den komponerade `adsTooBroadAdviceOnList + " " + tooBroadAdviceRemedy` är tecken för tecken den levererade `tooBroadAdviceOnList`. Samma sak på `/oversikt` (värdet oförändrat vid omdöpningen).
- **§5 `Tests:` klaras med marginal på båda ytorna:** premissen "räknat annonstal + vägrad matchning" produceras av `CriterionMatchingAdSetResolver.ResolveIdsAsync`, och jag har verifierat varje tal i kommentarerna mot `src/` — `MaxSetSize = 2_000` (rad 81/293), `CriterionAdMagnitudeDto.Ceiling = 10_000`, `MaxPerCriterion` = derived 2 500, och att första guarden (`magnitude.TooBroad`, rad 289) gör spegelvändningen onåbar. Inga fel siffror, ingen onåbar premiss.
- **Alla sju CTO-beordrade meningar är verifierat borta ur `src/`** (grep: enda träffarna är själva rapporten, som citerar dem som raderade), sv/en-nyckelparitet mätt till 126/126 i `oversikt.json` och 533/533 i `pages.json` med noll orphans, och `kommunSelectedCount`/`tooBroadAdviceOnList`/`criteriaSummary.tooBroadAdvice` har noll levande konsumenter kvar.

### Sammanfattning
0 blockers, 1 major, 3 minor. Major 1 är in-block och stängs **mekaniskt** genom radering (ingen omkontroll hos mig behövs då); skrivs kommentaren om i stället går fix-deltat tillbaka till mig, report-only, scopat till deltat (CLAUDE.md §9.6). Minor 2–4 routas per §9.6: fix in-block eller namngiven skip i PR-kroppen (Minor 3 är den enda med varaktigt värde). Inget §12-klass i deltat — ingen CSS, ingen design-token, inget `any`, ingen `useEffect`-hämtning, ingen ny klientkomponent, inget nytt bibliotek; copy följer §10 (du-form, inga utropstecken, inga emoji). Ingen konflikt mot CTO-domslutet: gate-uttrycken, nycklarna och de exakta strängarna är implementerade som beslutat, inklusive de identiska sv-pluralarmarna och den medvetet orörda `sniSelectedCount` för `/foretag/sok`.

---

**Escalations:** none. **Issues filed:** none.

---

# Scoped re-check (round 2) — HEAD 6cb60a5c, scope `git diff 804d9eff..6cb60a5c`

## Code-review: omkontroll av fix-deltat, PR #1725 (#1715 a+b, #1711) — round 2
**Status:** ⚠ Changes requested
**Auktoritet:** CLAUDE.md (AGENTS.md) §5 `Comments:`, §5 `Tests:`, §4, §7, §12; CLAUDE.md §9.6
**Scope:** Report-only, scopat till `804d9eff..6cb60a5c` (12 web-filer + 4 promotade rapporter). Områdena 1–3 triggas inte — ingen `src/`-fil ändrad; `CriterionMatchingAdSetResolver.cs` läst som mätunderlag. Fyra testfiler körda: `Test Files 4 passed (4)`, `Tests 47 passed (47)`, vitest 4.1.10.

### Round-1-fynden

1. **Major 1 — STÄNGT.** `criteria-section.test.tsx:214`: `grep '|| i.matching' src/` och `grep 'i\.matching\.tooBroad`' src/` ger båda **noll** träffar; den tillagda raden bär samma mening minus parentesen och inget nytt påstående (gatens andra term är fortfarande `anyMatchingOnlyTooBroad`, `criteria-section.tsx:44`).
   ⚠ **Mätkorrigering till promptens premiss:** stängningen var **inte** mekanisk. `git diff --numstat 804d9eff..6cb60a5c -- .../criteria-section.test.tsx` → `1  1`, och `+`-rader utöver headern = **1**, inte 0. §9.6:s mekaniska form kräver noll tillagda rader, så en scopad omkontroll hos mig återstod — den här är den, och fyndet är stängt i den. Ingen ytterligare rond ägs av mig för detta.
2. **Minor 2 — STÄNGT.** `adsTooBroadShort` i `messages/{sv,en}/pages.json:478` + två anropsställen (`criterion-ad-lines.tsx:213`, `:226`). Okvalificerat `tooBroadShort` greppar till noll i `src/` och `messages/`. Nyckelparitet ommätt: `pages.json` 533/533, `oversikt.json` 126/126, noll orphans.
3. **Minor 3 — STÄNGT, och kommentaren är verifierat sann.** `criterion-dialog.test.tsx:81–92`: `.slice` borta, `p.font-medium` borta. Båda leden i den nya kommentaren stämmer: `criterion-picker.tsx:181` (`<section role="group" aria-label={groupAria}>`) **och** `criterion-tree.tsx:48` (`<div role="group" aria-label={groupAriaLabel}>`) bär samma namn — därav `getAllByRole` + `.find`; filterstatusen `criterion-picker.tsx:234-235` är `role="status" aria-live="polite"`, räknaren `:256-257` är `aria-live="polite"` utan `role`, så `:not([role])` diskriminerar; precedensen finns på `criterion-picker.test.tsx:239`. Ingen `aria-live` i `criterion-tree.tsx` som kan kollidera.
4. **Minor 4 — STÄNGT.** Blankrad `criteria-card.test.tsx:231`, tankstreck `:207-208`.

### Major (ny i deltat)

5. **Faktiskt felaktig kommentar: fel guard och fel tal — NY I DELTAT** — Fil: `web/jobbliggaren-web/src/app/(app)/foretag/branschbevakningar/[id]/page.test.tsx:319-321`
   Nuvarande: `// Guard 3 of CriterionMatchingAdSetResolver.ResolveIdsAsync: the matching set refused above MaxSetSize on a COUNTED magnitude (the fixture's 12).` Guard 3 är `if (magnitude.Magnitude > MaxSetSize)` (`CriterionMatchingAdSetResolver.cs:293`, `MaxSetSize = 2_000` rad 81). Fixturen sätter `ads: { magnitude: 12, saturated: false }` (`page.test.tsx:279`), så predikatet `12 > 2000` är falskt och **guard 3 kan inte fira på den fixturen**. Den guard som producerar `magnitude` räknad + `matching.tooBroad` vid magnitud 12 är i stället guard 4 (`resolved.Refused || resolved.State == TooBroad`, rad 305) — portens vägran, som metodens egen docblock beskriver: *"the port still asks for one row more than it can accept, so a set that grew between the count and the query is refused rather than truncated"*. Exakt samma tillstånd i `criteria-card.test.tsx:204-208` är korrekt namngivet som guard 3, eftersom den fixturen är `counted(3000)`.
   ⚠ §5 `Tests:` **klaras** — premissen är producerbar via guard 4, så detta är ingen onåbar premiss och ingen Blocker.
   Krävs: fel guard-namn och talet bort. Antingen guard 4 + växt-racet namngivet, eller — mekanisk stängningsform — radera den felaktiga halvan (`Guard 3 of … (the fixture's 12).`) och behåll den sanna andra meningen; strängen `Guard 3` greppar då till noll i filen och stängande diff lägger noll rader (§9.6, ingen ny omkontroll hos mig).
   Motivering: CLAUDE.md §5 `Comments:` — "a factually wrong comment — wrong number, wrong gate name, stale §-reference — is a defect and is fixed"; min skala graderar det Major (ej §12-STOPP: `Comments:` är den carve-out §12 namnger).
   Delegera till: test-writer.

### De andra agenternas delta (endast min egen skala)

- design-reviewer Major (sv+en `ads.matchingTooBroad` / `ads.matchingTooBroadOnList`), Minor 1 ("av dem"), Minor 2 (breadth-docblock), dotnet-architect NTH-2 ("här"-halvan), NTH-5 (breadth-assertions ur dialogtestet): **inget fynd i min skala.** Mätt: den ads-armade satsen finns kvar i exakt fem sv-hem och fyra en-hem, **samtliga ads-arm-nycklar** där den är sann (`adsTooBroad`, `tooBroadNoList`, `adsAndMatchingTooBroad`, `adsTooBroadShort`, `adsTooBroadAdvice`) — noll matchnings-arm-nycklar bär den. Breadth-pinnen som togs ur dialogtestet har ett levande hem: `criterion-breadth.test.tsx`.
- dotnet-architect NTH-3 (`criteria-card.tsx:154`): pekaren är sann — `criteria-section.tsx:41-42` bär faktiskt "Parity `criteria-card.tsx`, deliberately not extracted". Pekare-till-pekare, men inget felaktigt påstående → ingen Major; frasering graderas inte i en omkontroll.
- Deltat är fritt från §12-klasser: ingen CSS, ingen design-token, inget `any`, ingen `console.log`, ingen `useEffect`-hämtning, ingen ny `"use client"`, inget nytt bibliotek.

### Bra gjort
- Den nya N = 1-karttesten (`criteria-card.test.tsx:121-128`) vilar på `counted(3000)` — samma producerbara guard-3-tillstånd som N ≥ 2-fallet, och den asserterar både den sanna och den falska satsen (positiv + negativ kontroll i samma test).
- Minor 3:s omskrivning ankrade i semantik som redan fanns och som redan hade en precedens i systersviten, i stället för att införa en ny selektor-idiom.

### Sammanfattning
0 blockers. Alla fyra round-1-fynden stängda och verifierade med samma mätning som etablerade dem (Major 1 dock **via denna omkontroll**, inte mekaniskt — promptens "0 added" är fel, `+`-rader = 1). **1 ny Major i deltat** (fynd 5, `page.test.tsx:319-321`), som per §9.6 stängs genom **radering** av den felaktiga halvan — då tillförs inga rader och ingen ytterligare omkontroll hos mig ägs. Skrivs kommentaren i stället om till guard 4 tillförs prosa, och då är taket nått: en fix som kräver ny prosa efter en omkontrollrond är STOPP till Klas + `blocked` (§9.6), inte en tredje rond. `agents-done` får inte sättas medan fynd 5 står öppet (§6, §12).

**Escalations:** none. **Issues filed:** none.

---

## Session's closure of finding 5 (mechanical, §9.6)

The three comment lines at `page.test.tsx:319-321` are deleted whole — the true second sentence
spans a line with the false first one, so keeping it would re-emit a line (a `+` line, as this
re-check measured for Major 1). The test's title and its two assertions carry the meaning. Closing
diff for that file: `0 added / 3 deleted`; `grep -c "Guard 3" page.test.tsx` → 0. Recorded in the
PR-body verdict table; no further re-check owed.
