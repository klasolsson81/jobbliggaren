# dotnet-architect — PR #1725 (#1715 a+b, #1711), round 1

- **Date:** 2026-09-14
- **HEAD reviewed:** 804d9eff (base a2ef342e)
- **Mode:** report-only (invoked for > 5 files, CLAUDE.md §9.2); transcribed verbatim by the driving
  session. Kritiskt/Viktigt/Nice-to-have map to Blocker/Major/Minor (§9.6).

---

## Arkitektur-analys

### Sammanfattning
Behöver åtgärdas — 0 kritiska, 1 viktigt, 5 nice-to-have fynd. Deltat är frontend-only (ingen C#, inga migrationer, inga `.csproj`/`Directory.Packages.props`-ändringar), så Clean Architecture-axlarna i AGENTS.md §2.1 är inte engagerade; granskningen gäller knowledge-piece-gränser (DRY/ubiquitous language) och om implementationen matchar CTO:ns bundna form. Den bundna formen är **uppfylld** — gate-uttrycken är byte-identiska på båda ytorna, remedy är en sträng läst av båda armarna, och ads-only-armen är pinnad ordagrant (`criteria-section.test.tsx:196-198`). Det viktiga fyndet är att #1715:s premiss stängs i ett av tre hem.

### Fynd

**[Viktigt]** `web/jobbliggaren-web/src/components/company-criteria/criterion-ad-lines.tsx:270` + `src/app/(app)/foretag/branschbevakningar/[id]/annonser/page.tsx:264-266`
**Vad:** Den falska meningen #1715 handlar om lever kvar i **långformen** och i list-formen. `ads.matchingTooBroad` börjar "Bevakningen matchar fler företag än vi kan räkna annonser för." och `ads.matchingTooBroadOnList` likaså — båda för en guard-3-refusal, där företagsaxeln inte är refuserad och annonstalet är räknat och utskrivet strax ovanför. Deltat bytte bara kortformen (`tooBroadShort` → `matchingTooBroadShort`).
**Varför:** Mätt, inte härlett. `shortRefusal = adviceStatedByCaller` (`criterion-ad-lines.tsx:157`), och `adviceStatedByCaller={false}` på alla tre N=1-ytor (detaljsidan `[id]/page.tsx:184`, `/oversikt` N=1 `criteria-card.tsx:131`, katalogen N=1) — så långformen är den de ytorna renderar, med "3 000 aktiva annonser från dessa företag" på raden ovanför. På annonssidan är det värre: `magnitudeUnanswerable = magnitude.tooBroad || magnitude.notMaterialised` (`annonser/page.tsx:190`) och raden är gatead `matching?.tooBroad && !magnitudeUnanswerable` — **det enda nåbara tillståndet är guard 3**, så meningens första sats är falsk av konstruktion, under en `h2` som skriver ut talet (`browse.magnitudeHeadline`). Samma nåbarhetsmätning som PR:en redan gjort för kortformen (`ResolveIdsAsync` guard 3, `MaxSetSize` 2 000 mot `Ceiling` 10 000) gäller dessa ytor. Ubiquitous language (Evans): efter deltat säger samma tillstånd en sann mening på två ytor och en falsk på tre — ett knowledge piece med två motstridiga hem, vilket är det CTO:ns DRY-motivering bygger på.
**Föreslagen åtgärd:** Stäng de två återstående hemmen under #1715 — antingen in-block (två nya/omskrivna `matchingTooBroad`/`matchingTooBroadOnList`-strängar sv+en som namnger annonsaxeln, precis som `matchingTooBroadShort` gör) eller i en följd-PR. **Routing är ingen sessionsbeslut här:** CTO-domen scopade (b) till block-gaten och ruled ingen copy för dessa två nycklar, och §9.6 lägger genuint tvetydig routing på `senior-cto-advisor`. Det som inte går är att stänga #1715 med dessa två levande — då stängs issuen mot en omätt yta.

**[Nice-to-have]** `web/jobbliggaren-web/messages/sv/pages.json:479` (`foretag.criteria.ads.tooBroadShort`)
**Vad:** `tooBroadShort` står nu okvalificerad intill den nya `matchingTooBroadShort`, i en familj där varje annan syskonnyckel är kvalificerad (`adsTooBroad`, `matchingTooBroad`, `adsAndMatchingTooBroad`, `adsTooBroadAdviceOnList`).
**Varför:** Exakt det skäl CTO:n själv angav för `tooBroadAdvice` → `adsTooBroadAdvice`: "an unqualified sibling name beside `matchingTooBroadAdvice` reads as the general case". Asymmetrin skapades av deltat (commit `c1209391`), som inte täcks av domen.
**Föreslagen åtgärd:** Noteras med en reservation: strängen tjänar **två** armar (`sharedRefusal === "tooBroad"` på rad 212 och `ads.tooBroad` på rad 225), så `adsTooBroadShort` vore fel för den delade armen. Antingen ett namn som bär det (t.ex. `adsCountRefusedShort`) eller lämna som är — men då är det en medveten asymmetri utan hem. Route per §9.6: issue eller namngiven skip i PR-kroppen.

**[Nice-to-have]** `web/jobbliggaren-web/src/components/company-criteria/criteria-section.tsx:97`
**Vad:** "…those reword the refusal the rows already carry above it, and **their** 'här' points at another block." Bara `adsTooBroadAdvice` innehåller "här" ("får inga tal här"); `matchingTooBroadAdvice` innehåller inget "här".
**Varför:** CLAUDE.md §5 `Comments:` — en faktiskt felaktig kommentar är en defekt; pluraliseringen gör påståendet falskt om en av de två meningarna. (Graderas i `code-reviewer`s charter; aldrig STOPP.)
**Föreslagen åtgärd:** Attribuera "här" till ads-meningen, eller stryk den halvan av satsen (en strykning skapar inget nytt påstående, §9.6).

**[Nice-to-have]** `web/jobbliggaren-web/src/components/oversikt/criteria-card.tsx:152-154`
**Vad:** Tvillingen `criteria-section.tsx:39-41` bär pekaren "Parity `criteria-card.tsx`, deliberately not extracted — see the prop's docblock". Kortet bär ingen sådan pekare — varken före eller efter deltat.
**Varför:** Den medvetna duplikationen (CTO: identiska uttryck, inte extraherade) fördubblades i detta delta från **ett** uttryck till **två**. En läsare som redigerar kortets gate har ingen signal om tvillingen. Bindet är dokumenterat ensidigt.
**Föreslagen åtgärd:** En pekare i kortets kommentar (samma mening som systern), eller lämna och namnge skippen. Drift fångas idag av test på båda sidor (`criteria-card.test.tsx` collapse-guard + `criteria-section.test.tsx`), vilket är varför detta är nice-to-have och inte högre.

**[Nice-to-have]** `web/jobbliggaren-web/src/components/company-criteria/criterion-dialog.test.tsx:85`
**Vad:** `counters()` selekterar `p.font-medium[aria-live="polite"]` — en Tailwind-utility som selektor i ett copy-test — och `.slice(0, 2)` skyddar inget (selektorn ger exakt 2 idag).
**Varför:** Repot har redan en dokumenterad selektor för samma element en fil bort: `criterion-picker.test.tsx:239` (`p[aria-live='polite']:not([role])`, med egen kommentar om varför inte bara `p[aria-live]`). En ren stilredigering (`font-medium` bort) fäller ett test om copy. Failar dock högt, inte tyst — därav graden.
**Föreslagen åtgärd:** Scopa per picker via dess `heading` med `within(...)`, eller återanvänd precedensselektorn och exkludera fotens `aria-live`-rad (`criterion-dialog.tsx:216-218`) på attribut i stället för på klass.

**[Nice-to-have]** `web/jobbliggaren-web/src/components/company-criteria/criterion-dialog.test.tsx:129-133`
**Vad:** Dialogtestet renderar `CriterionBreadth` och asserterar dess copy ("1 bransch · 2 kommuner" / "4 branscher · 2 kommuner").
**Varför:** Den regeln har redan ett hem — `criterion-breadth.test.tsx:62-68` ("räknar RÅA löv"), som i detta delta dessutom fick resolutionsmeningen. Ett andra hem gör att dialogtestet faller när breadth-copyn ändras, av ett skäl som inte rör dialogen (DRY: ett hem per knowledge piece).
**Föreslagen åtgärd:** Behåll kontrast-kommentaren (den är värdefull och pekar rätt), stryk de två `breadth(...)`-assertionerna och `breadth`-hjälparen; dialogens halva (`["1 val","1 val"]` för både ett löv och hela huvudgruppen) bär redan poängen.

### Svar på de tre frågorna

**(a) Ingen DRY-överträdelse.** De två `.filter().join(" ")`-blocken är *incidental duplication*, inte ett delat knowledge piece: två meningsuppsättningar (2 respektive 3 element), två namespaces (`oversikt.criteriaSummary` vs `pages.foretag.criteria.ads`), två avslutande noder (`Link` vs inget) och två ändringsskäl (katalogens remedy vs `/oversikt`:s CTA). Det som *är* ett knowledge piece är gate-uttrycken, och de är CTO-bundna till att vara identiska och pinnade på båda sidor. En extraktion måste bära båda meningsuppsättningarna och skillnaden i sista noden, dvs. en komponent med en surface-switch — precis den `variant="summary"`-lukt familjen redan pensionerade (`criterion-ad-lines.tsx` docblock, D1/in-block 2). Enda detaljen: i `criteria-section.tsx:105` är tredje elementet (`tooBroadAdviceRemedy`) aldrig `null`, så `filter`:ets kontrakt är "de två första kan vara null" — korrekt men outtalat; inget fynd.

**(b) Nyckelnamnen är konsekventa med den levererade konventionen.** `…OnList` är redan polysemt i samma namespace före deltat — fyra nycklar betyder "på annonslistan" (`matchingTooBroadOnList`, `matchingNotAssessedOnList`, `matchingNotMaterialisedOnList`, läst av `[id]/annonser/page.tsx`) och två betyder "på bevakningskatalogen" (`adsTooBroadAdviceOnList`, `matchingTooBroadAdviceOnList`, läst av `criteria-section.tsx`). Deltat ärver det, skapar det inte. `tooBroadAdviceRemedy` utan suffix är rimligt: den är ett *fragment* som båda armarna läser, inte en ytspecifik mening — och CTO:n namngav nyckeln explicit. Inget fynd. Asymmetrin som *är* ny är `tooBroadShort` (fynd 2 ovan). sv/en-nyckelparitet mätt: 0 drift över alla 23 katalogfiler; `kommunSelectedCount` borttagen i båda locales med noll kvarvarande konsumenter; `sniSelectedCount` kvar med sina två `/foretag/sok`-konsumenter (`bransch-popover.tsx:105`, `foretag-sok-searchbar.tsx:483`).

**(c) Mockarna är korrekta, två selektor-/scope-detaljer är det inte** (fynd 5 och 6). `useCriterionPreviewCount`-stubben returnerar exakt hookens deklarerade form (`{preview: PreviewMagnitude | null, loading: boolean}`) och `{preview: null, loading: false}` är ett tillstånd produktionen själv emitterar (båda axlarna krävs; hooken anropar inte förrän varje axel har en kod — hookens eget kontrakt, rad 22-24). Ingen CLAUDE.md §5 `Tests:`-överträdelse: ingen produktionsfakta asserteras på en premiss produktionen inte kan producera. Server-action-mocken utelämnar `deleteCriterionAction`, vilket är ofarligt (dialogen importerar den inte; ett framtida anrop failar högt). Fixturens påstående "Huvudgrupp 62 carries FOUR leaves, as in `sni-2025.v1.json`" är **mätt sant** (62100, 62201, 62202, 62900), och J/"Informations- och kommunikationsverksamhet" följer familjens etablerade fixturkonvention (`criteria-section.test.tsx:72`, `criterion-picker.test.tsx:23`) — ingen avvikelse att rapportera. `"Rensa val"` kommer från `components.criterionPicker.clear`, oförändrad av deltat, så den assertionen är en pin på befintlig vokabulär och inte ett nytt påstående — bra.

### Referenser
- AGENTS.md §2.1 — EF Core/lagerregeln: ej engagerad (frontend-only delta)
- AGENTS.md §2.2 / §4 — ubiquitous language, Server Components/klientmutationer
- CLAUDE.md §5 `Comments:` (faktafel = defekt) och §5 `Tests:` (premiss produktionen kan producera)
- CLAUDE.md §9.6 — routing; Viktigt = Major (in-block eller följd-PR, aldrig issue), Nice-to-have = issue eller namngiven skip under backlog-taket
- `docs/reviews/2026-09-14-1715-1711-senior-cto-advisor.md` — den bundna formen (G2 split gate, B split key)
- `src/Jobbliggaren.Application/CompanyWatches/Queries/CriterionMatchingAdSetResolver.cs:286-306` — guard 1 respektive guard 3, grunden för `ads.tooBroad ⇒ matching.tooBroad`

**Eskaleringar:** inga (ingen §12-klass i deltat — ingen §5-antipattern, ingen lagergräns, inget nytt bibliotek, ingen design-token utanför DESIGN.md). **Issues filade:** inga (read-only charter).
