# code-reviewer — #1736 (epic #1732 part 1b): PR #1751

**Date:** 2026-09-17 · **Agent:** code-reviewer · **PR:** [#1751](https://github.com/klasolsson81/jobbliggaren/pull/1751)
· **Head reviewed:** `41aa7def` (the brief named `402fe30a`; the agent measured the moved head and
reviewed `88b6ea93..41aa7def`) · **Mode:** report-only · **Verdict:** ✓ Approved, 0 Blocker / 0 Major /
3 Minor. Report returned in the reply and transcribed verbatim by the session.

---

## Code-review: terms-acceptance stamp on `JobSeeker.Register` + migration (PR #1751, #1736, epic #1732 part 1b)
**Status:** ✓ Approved (0 Blocker, 0 Major, 3 Minor)
**Auktoritet:** CLAUDE.md §§2.1, 2.2, 2.3, 2.4, 3, 3.6, 4, 5 (`Comments:`, `Tests:`, `Security:`), 7, 9.6, 12
**Scope:** Domain · Application · Infrastructure (EF config + migration) · Api/Worker integration tests · Architecture registries · Next.js server action + `messages/{sv,en}` · ADR 0142 D6

⚠ **HEAD moved past the reviewed range.** The brief named `402fe30a`; the worktree HEAD is
`41aa7def` — a fifth commit, `test(web): the Playwright register helper and the gate probe carry
acceptTerms (#1736)`, touching `web/jobbliggaren-web/tests/e2e/helpers/auth.ts:70` and
`docs/runbooks/registration-gate.md:309`. **I reviewed `88b6ea93..41aa7def`**, and that commit is
correct and closes the last two unswept `POST /api/v1/auth/register` bodies in the repo (I re-swept:
22 files mention the route; every one that sends a body now carries `acceptTerms`). The session's
measured suite numbers were taken at `402fe30a`; the fifth commit touches only a Playwright helper
and a runbook, so the .NET/vitest figures stand. Per CLAUDE.md §9.6 the session must still verify HEAD
is unchanged immediately before setting `agents-done`.

### Blockers
None.

### Major
None.

### Minor

1. **ADR line pointer frozen in a sentence the same PR moved** — Fil: `C:/tmp/jbl-1736/docs/decisions/0142-passwordless-auth-one-page-code-or-link-oauth-ready.md:273`
   Nuvarande: the rewritten D6 sentence keeps "the cost is one call site in `src/`
   (`RegisterCommandHandler.cs:113`)". Measured: after this PR line 113 is the first line of the
   `#1736` comment; the call is at `RegisterCommandHandler.cs:116–117`.
   Krävs: drop the line number (`RegisterCommandHandler.cs`, file only), or state the regenerating
   grep. The amendment three paragraphs below (`:308–310`) replaced the *count* with
   `git grep -c 'JobSeeker\.Register(' -- src tests` on exactly the grounds that a frozen number
   decays — the line pointer in the same sentence is the other frozen number, and this PR is what
   invalidated it.
   Motivering: AGENTS.md §5 `Comments:` ("a live measured number in a tracked file … publish the
   command that regenerates it"); code-reviewer charter grades comment accuracy.
   Delegera till: dotnet-architect (owns the D6 form it ratified) — one-token deletion.

2. **"no setter reaches it" overstates what the PR's own pin asserts** — Fil: `C:/tmp/jbl-1736/src/Jobbliggaren.Domain/JobSeekers/JobSeeker.cs:18-19`
   Nuvarande: "Stamped once by `Register`, never updated: no setter reaches it and no method
   rewrites it." The property at `:25` is `{ get; private set; }`, and
   `JobSeekerTests.TermsAcceptance_HasNoPublicWritePathOnTheAggregate` (`JobSeekerTests.cs:442-443`)
   asserts the setter **exists** and is non-public.
   Krävs: "no accessible setter reaches it" (or "no public setter"), so the comment and the pin say
   the same thing. The second half ("no method rewrites it") is exact and pinned at
   `JobSeekerTests.cs:450-455`.
   Motivering: AGENTS.md §5 `Comments:` — phrasing, not a defect; "en kommentar är ingen bugg".
   Delegera till: dotnet-architect.

3. **Test helper closes the DbContext's own connection instead of newing its own** — Fil: `C:/tmp/jbl-1736/tests/Jobbliggaren.Api.IntegrationTests/Persistence/TermsAcceptanceBackcompatTests.cs:61-75`
   Nuvarande: `NullTermsColumnsAsync` takes `(NpgsqlConnection)db.Database.GetDbConnection()`, calls
   `OpenAsync` at `:66` and `CloseAsync` at `:75`. Benign here (the scoped context is used for
   nothing else), but it takes the connection's open state out of EF's hands.
   Krävs: the form this PR's own sibling uses — `AddTermsAcceptanceToJobSeekerMigrationTests.ReadAddedColumnsAsync`
   (`tests/Jobbliggaren.Worker.IntegrationTests/Migrations/AddTermsAcceptanceToJobSeekerMigrationTests.cs:72-75`)
   news an `NpgsqlConnection` under `await using` and never touches EF's.
   Motivering: CLAUDE.md §9.1 "check existing patterns (reuse, don't invent)"; §3.6 keeps the port at
   `IAppDbContext`. The SQL itself is correctly parameterized (`:73`) — no injection surface.
   Delegera till: test-writer.

### Bra gjort
- **§5 `Tests:` premise discipline is exemplary** — `TermsAcceptanceBackcompatTests:19-31` names the actor for the all-NULL row (rows written before `20260917141433`), states why it is not callable, and names the two pins that live with the current writer; `RegisterCommandHandlerTests.cs:801-805` declares in writing why there is *no* handler test for `AcceptTerms=false` rather than asserting off a state the pipeline cannot produce.
- **The sweep is measured, not asserted** — I re-counted with a paren-matching parse: 324 `JobSeeker.Register(` call sites in `tests/`, **321 passing `TermsAcceptance.AcceptCurrent`**, the other three being the two deliberate `null!` refusals and the one that names its own `acceptance`. The 17 hoisted `var clock = …` sites preserve one-clock semantics (`CryptoErasureHardDeleteTests.cs:111-114`, `AuthTestHelpers.cs:80-81`); the Register signature has exactly one production caller.
- **Copy first, constant follows** — `privacy.updated` bumped to `2026-09-17` in *both* locales with the Art. 13 disclosure sentence added to both, then `CurrentPrivacyPolicyVersion` follows, and `TermsAcceptanceVersionsMatchPublishedPolicyTests` makes the pair checkable in both locales plus an ISO-shape arm that survives an `EndsWith` that drifted to a non-date.

### Sammanfattning
0 Blockers, 0 Major, 3 Minor → merge is not blocked (CLAUDE.md §6/§12: no unresolved
Blocker/Major, no §5 STOPP class, no Clean-Architecture boundary crossed). **Verified, not
inherited:** Domain takes no EF Core/Mediator/FluentValidation (`TermsAcceptance.cs` imports only
`Jobbliggaren.Domain.Common`); Application gains no provider package; `OwnsOne` +
`Navigation(...).IsRequired(false)` matches the `ManualPosting`/`AdSnapshot` precedent at
`ApplicationConfiguration.cs:70,122`; the private-ctor parameter names match the properties, so EF's
constructor binding materializes it, and the round-trip is proven at
`TermsAcceptanceBackcompatTests.cs:120-136`; migration `Up` is three nullable `AddColumn`s with a
`Down` that is actually exercised head→previous→head; `ErasureCascadeRegistry.cs:505-508,789-794`
classifies both version columns `NotRecruiterData` with the parity column
(`resume_files.pnr_consent_dialog_version`) confirmed present at `:587`; every factual number and
name I could check in the new comments holds — migration id, "five `updated` dates" (5 per locale,
measured), "the #1117 form above" (referenced at `RegisterCommandValidator.cs:16`), "`terms_accepted_at`
is timestamptz and outside the text sweep". Swedish copy is "du", no emoji, no exclamation marks.
Delegations: 2 × dotnet-architect (Minor 1, 2), 1 × test-writer (Minor 3).
**Routed to security-auditor, not graded here** (CLAUDE.md §9.2 — she is mandatory on this PR
anyway): hard delete removes the whole `job_seekers` row (`AccountHardDeleter.cs:269`), so the
Art. 5(2) acceptance record the ADR names is erased with the account. Deleting is defensible and
probably required under Art. 17, but the ADR asserts the row *is* the accountability record without
saying that — her call, not mine.
Re-review efter fix: samma agent, report-only, scopad till fix-deltat (CLAUDE.md §9.6).

---

## Scoped re-check (2026-09-17) — fix delta `41aa7def..c2c355d2`, report-only, same agent

**Verdict:** 3/3 prior Minors CLOSED (each by its own grep) · 2 new-in-delta Major — (1) a falsified
clause in the configuration comment, closed by deletion (no re-check owed); (2) the migration agent's
APPROVE had to reach the re-scaffold — it already did (its scoped re-check on `c2c355d2` is appended to
`docs/reviews/2026-09-17-1736-db-migration-review.md`; the code-reviewer read the report before that
append). Transcribed verbatim from the agent's reply.

### Code-review: scoped re-check of the fix delta (PR #1751, #1736) — report-only
**Status:** ⚠ Changes requested (3/3 prior Minors closed · 2 new-in-delta Major)
**Auktoritet:** CLAUDE.md §§5 (`Comments:`), 6, 9.2, 9.6, 12
**Scope:** `git diff 41aa7def..c2c355d2` only (19 files). HEAD verified at `c2c355d2` before and after this read. No edits, no commits, no suites run. Grading is per-delta; the earlier round's verdict on untouched code stands.

#### Closure of the three Minors — measured, not taken on report

| # | Finding | Status | Measurement |
|---|---|---|---|
| 1 | ADR line pointer `RegisterCommandHandler.cs:113` | **Closed** | `grep -c "RegisterCommandHandler.cs:113" docs/decisions/0142-…md` → **0**; `:273` now reads "(`RegisterCommandHandler.cs`)". Closes mechanically — the string is gone and no claim replaced it. |
| 2 | "no setter reaches it" vs the PR's own pin | **Closed** | `JobSeeker.cs:19` now "no **public** setter reaches it"; `grep -c "public setter reaches it"` → **1**. Now says exactly what `JobSeekerTests.cs:441-443` asserts. |
| 3 | Test helper closed EF's own connection | **Closed** | `grep -c GetDbConnection tests/…/TermsAcceptanceBackcompatTests.cs` → **0**. `ExecuteAsync` (`:76-87`) news its own `NpgsqlConnection` from `GetConnectionString()` under `await using`, still parameterized (`:85`). It is now the shared seam the new fourth fact also uses — a better outcome than the fix I asked for. |

#### Major (new-in-delta)

1. **The delta introduces a measured claim that contradicts a standing production comment; one of the two is false** — Fil: `C:/tmp/jbl-1736/tests/Jobbliggaren.Api.IntegrationTests/Persistence/TermsAcceptanceBackcompatTests.cs:16-18` vs `C:/tmp/jbl-1736/src/Jobbliggaren.Infrastructure/Persistence/Configurations/JobSeekerConfiguration.cs:40-42`
   Nuvarande: the delta adds "*Measured 2026-09-17: EF reads an all-null owned row as `null` **with or without** `Navigation(...).IsRequired(false)`*". `JobSeekerConfiguration.cs:41-42` still says "*and **without it EF cannot tell** "no stamp" … from an all-null instance*". Same mechanism, opposite verdicts — they cannot both hold, and **the delta is what created the contradiction**, which is why I report it inside the re-check scope rather than as a finding on untouched lines.
   Krävs: delete the falsified clause — from "and without it EF cannot tell" (`:41`) through "from an all-null instance." (`:42`). "`Navigation.IsRequired(false)` is obligatory, the ManualPosting/AdSnapshot form: EF Core 10 defaults an owned reference to required" (`:39-41`) survives intact and is still true; what the delta measured is that the call earns its keep at **scaffold** time (nullable columns), not at read time — and that reason is already written, correctly, in the test docblock. Nothing new needs saying in the config comment.
   Motivering: AGENTS.md §5 `Comments:` — a factually wrong comment is a defect and is fixed; code-reviewer charter grades it Major. CLAUDE.md §9.6 new-in-delta route: **closed by deletion** (a deletion introduces no claim), recorded as one verdict-table row with `grep -c 'without it EF cannot tell' src/` → 0. No further re-check owed from me on this finding.
   Delegera till: dotnet-architect. *(Note, not a second finding: the same proposition is in `docs/reviews/2026-09-17-1736-architect-form.md:60`. That is a dated agent report — §1.6 provenance, left as written — but it is where the claim came from, so do not re-derive it from there.)*

2. **The mandatory migration agent's APPROVE no longer covers the migration that will merge** — Fil: `C:/tmp/jbl-1736/src/Jobbliggaren.Infrastructure/Persistence/Migrations/20260917153605_AddTermsAcceptanceToJobSeeker.cs:33-37,45-47` and `C:/tmp/jbl-1736/docs/reviews/2026-09-17-1736-db-migration-review.md:3-7,71`
   Nuvarande: the promoted report states "**Head reviewed: `402fe30a`**" and its verdict table certifies "*Migration = 3 nullable AddColumn / 3 DropColumn, **inget annat*** ✓". The delta re-scaffolded the migration under a **new id** (`20260917141433` → `20260917153605`) and gave `Up` an `AddCheckConstraint` and `Down` a `DropCheckConstraint`, plus the constraint in the Designer and `AppDbContextModelSnapshot.cs:622-625`. The report's session note (`:69-72`) clears only `41aa7def`, "which touches no migration" — it does not reach `c2c355d2`. So the one fact that report certifies is now false about the artifact that merges.
   Krävs: a scoped, report-only re-check by **`db-migration-writer`** against `20260917153605` before `agents-done`, and its outcome recorded in the report (session note or appended verdict) so the record stops asserting a false fact. Within cap: that agent has had one round and zero re-checks (CLAUDE.md §9.6).
   Motivering: CLAUDE.md §9.2 (`db-migration-writer` mandatory for new migrations) and §6 — a push carrying content means the reviewers answered against a diff that is no longer the one merging; §12 keeps the gap merge-blocking. I am **not** grading the migration's content: severity there belongs to `db-migration-writer`'s scale (§9.6), and what I read of it looks right — naming matches the `ck_taxonomy_snapshot_meta_singleton` precedent, `num_nonnulls` never returns NULL so the constraint cannot pass vacuously, and no path in `src/` or `docs/runbooks/account-deletion.md` nulls a single terms column.
   Delegera till: db-migration-writer (via the session).

#### Bra gjort
- **The migration test now measures the deploy's case, not an empty table** (`AddTermsAcceptanceToJobSeekerMigrationTests.cs:132-153,213-217`): a row inserted *at the previous migration* rides the forward step and its three columns are read NULL — which catches a `defaultValue` that `is_nullable` alone cannot see, and proves `ADD CONSTRAINT` validated the legacy all-NULL shape. Its NOT NULL claim (`:106-111`) I checked against the snapshot: id, user_id, display_name, preferences, created_at, `match_preferences` defaulted — exact.
- **The corrected docblock is the right kind of correction**: it replaced a claim the session could not support with a dated measurement *and* said what the call actually buys, rather than quietly deleting the sentence. Finding 1 exists only because the other half of the pair was left standing.
- **Arm (3) closes the hole arms (1)+(2) left** (`JobSeekerTests.cs:456-464`) — a `ReAcceptTerms(clock)` takes no `TermsAcceptance` and would have passed both; the special-name exclusion for the property accessor is stated and correct. `RegisterConfirmationTests.cs:216-234` adds the #714 fresh-vs-taken byte-parity oracle for `acceptTerms=false`.

#### Sammanfattning
3 Minors closed (each measured by its own grep, not by report). 2 new-in-delta Major, both merge-blocking
under §6/§12: one factually wrong comment closable **by deletion** with no re-check owed to me, and one
stale mandatory verdict that needs `db-migration-writer` re-run scoped to `20260917153605`. No other
new-in-delta finding: the check constraint, the BUILD.md sketch lines, the release-checklist deploy-order
paragraph, the `MalformedJsonbSeedTestBase` header and the ADR amendment sentences all check out against
their sources — I verified the constraint comment's "the account-deletion runbook updates `job_seekers`
directly" against `docs/runbooks/account-deletion.md:199,285,296` (true), and the stale
`20260917141433` id now survives only inside dated review reports, in no code and no live doc.
**My re-check cap for PR #1751 is now spent** (CLAUDE.md §9.6: one batched round + one scoped re-check).
Finding 1 is designed to close without returning to me; if instead it grows new prose, that is a STOPP to
Klas with the `blocked` label, not a third round.

### Session's closure of the two new-in-delta Majors

- **Major 1 — closed by deletion** in the follow-up commit: the clause from ", and without it EF cannot
  tell" through "from an all-null instance" is gone from `JobSeekerConfiguration.cs`;
  `git grep -c 'without it EF cannot tell' -- src` → 0. The dangling "That sentinel" became "The all-null
  sentinel" (the term `ApplicationConfiguration.cs` already uses) — a referent, not a claim.
- **Major 2 — already closed when reported:** db-migration-writer's scoped re-check of `20260917153605`
  on `c2c355d2` (APPROVE stands, four operations in each direction, `Down` order verified) is appended to
  `docs/reviews/2026-09-17-1736-db-migration-review.md`; the code-reviewer read the file before the append.
