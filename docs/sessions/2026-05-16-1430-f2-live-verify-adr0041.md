---
session: F2 Saved Searches live-verifiering + auth-gated visuell verifiering + ADR 0041 a11y-fix
datum: 2026-05-16
slug: f2-live-verify-adr0041
status: levererad (Fas 2 formell stängning PAUSAD — ingestion = separat session, Klas-beslut)
commits:
  - 12fc9e6 fix(web): WCAG 1.4.11 dark modal-border-token (ADR 0041)
  - 64a6bf8 feat(web): visual-verify auth-läge + dev-test-konto-protokoll
  - (docs-commit denna session)
tags:
  - v0.2.7-dev (29cd4ae — F2 backend+frontend + migration)
  - v0.2.8-dev (64a6bf8 — a11y ADR 0041 + visual-verify auth-läge)
---

# F2 live-verifiering + ADR 0041 — session-retrospektiv

## Mål

Deploya F2 Saved Searches, verifiera auth-gated UI visuellt mot live, rök-testa
end-to-end, formellt stänga Fas 2-milstolpen.

## Vad som levererades

**Deploy `v0.2.7-dev`** @ `29cd4ae` (Klas-GO). deploy-dev.yml auto-applicerade
migration `20260516092628_F2SavedSearches` (Phase E, CloudWatch EventId 63 —
`saved_searches` + index). /api/ready 200, HSTS.

**`visual-verify.ts` auth-läge (senior-cto-advisor Variant A):** opt-in via
env (`VISUAL_AUTH_EMAIL/PW/BACKEND_URL`, https-only `VISUAL_BASE_URL`). Direkt
backend-login → `__Host-jobbpilot_session`-cookie injicerad in-memory i
Playwright-context (aldrig storageState/disk — §5.4-risk eliminerad vid
källan). Temp-fixture-sökning skapas/raderas för populerade UI-tillstånd.
48 shots × 3 vp × light/dark. Runbook uppdaterad (tre-nivå-tabell +
env-kontrakt + dev-test-konto-sektion + Future-watch).

**Dev-test-konto + cred-persistens Variant C (senior-cto-advisor, Klas-GO):**
dedikerat syntetiskt JobSeeker-konto (skapat via direkt `/api/v1/auth/register`).
Creds i `%USERPROFILE%\.jobbpilot\dev-test-creds.env` — utanför repo-trädet
(noll repo-yta, defense-in-depth > gitignore-tillit). Runbook + MEMORY-pekare,
aldrig creds:en själva.

**a11y-Blocker → ADR 0041 (Accepted):** design-reviewer Approved-with-Minors;
Minor 1 (dark dialog svag) → nextjs-ui-engineer auktoritativ token-math →
bekräftad WCAG 1.4.11-Blocker i delad `ui/dialog.tsx` (dark dialogyta
`#020617` = dimmad canvas; enda kant `--jp-border` 1.35:1 < 3:1; ingen
befintlig låst `--jp-*`-kombination når 3:1). senior-cto-advisor Alt 2
(minsta-ingrepp) + Klas-GO på inriktning + tokenvärde + `globals.css`-diff:
nytt semantiskt token `--jp-border-modal` (light `#E2E8F0` / dark `#64748B` =
befintligt slate-500/`--jp-info-500`, ≈3.6:1) + `--color-border-modal`-bridge;
`DialogContent` `border-border` → `border-border-modal`. Alla dialoger fixade
via en token (DRY/SPOT). DESIGN.md/`tokens-full.md`/`contrast-table.md`
uppdaterade.

**Deploy a11y-fix + live-verifiering:** `git push origin main` (`64a6bf8`) →
Vercel auto-deploy frontend; backend-tag `v0.2.8-dev`. Verifierat: serverad
CSS har `--jp-border-modal:#64748b` + `border-border-modal`. Re-capture mot
live (`20260516-1424`) → design-reviewer re-review **0 Blockers/0 Major/0
Minor, Blocker RESOLVED, noll regression**. Klas slutgodkände bilderna.

**Rök-test live grönt:** login → create 201 → list → run **HTTP 200, paged
shape, totalCount=137** ("utvecklare") → scoping okänt-id **404** (ADR 0031
oskiljbart cross-tenant) → delete 204 → borttagen **404**.

## Beslut och avstickare

- **Auth-creds:** Klas valde att CC skapar eget dedikerat dev-test-konto
  (renare än delade creds). Verifierat: `/api/v1/auth/register` är **inte**
  `RegistrationsOpen`-flag-gejtat (kill-switch täcker bara waitlist/invite).
- **CTO Variant A** (utöka visual-verify, ej slängbart script/e2e-fundament)
  — DRY/CCP vs YAGNI/§5.4. **Variant C** för cred-plats (utanför repot).
- **a11y-Blocker eskalerad, ej deferrad** (§9.6): defekt i nuvarande-fas
  delad primitiv → fixas före fas-stängning, ej TD. Cross-cutting natur
  eskalerar (Klas-GO för token), deferrar inte.
- **DESIGN.md-guard:** harness-classifern blockerade CC från att själv-köra
  `approve-spec-edit.sh` trots Klas-GO via beslutsfråga. Ej forcerat (vore
  bypass). Klas körde approven manuellt → enradaren applicerad. Auktoritativ
  token-spec låg redan i skill-docs.
- **`__Host-`-cookie kräver https** → odeployad lokal fix kan ej capureras
  över http://localhost. Runbook-sanktionerad verifiering sker mot
  live-deploy ändå; https-guard tillagd i scriptet.

## Reviews

- senior-cto-advisor ×3 (auth-capture-approach, cred-persistens, a11y-Blocker-
  triage+fix-approach) — alla entydiga, INLINE före implementation.
- design-reviewer ×2 (initial Approved-with-Minors; re-review 0/0/0 RESOLVED).
- nextjs-ui-engineer ×1 (auktoritativ token-math — eskalerade Minor→Blocker).
- security-auditor ×1: PASS, 0 Crit/High/Med, 2 Low informativa (deleteFixture
  res.ok-check in-block-fixad; namn-match noterad).

## Fas 2-status

Milstolpen "söka jobb på Platsbanken + spara sökningar": auth-gated visuell
verifiering + rök-test = **gröna**. Formell stängning **PAUSAD** — gaten
"ingestion-cron verifierad" tillhör separat lokal session (Klas-beslut;
EventId 5402 + `job_ads`-korpus ~40k+). `run`=137 visar data finns men
ersätter inte cron/korpus-verifieringen.

## Pending / nästa session

- **F2 ingestion-cron-verifiering** (separat lokal session, AWS SSO):
  CloudWatch EventId 5402-completion + `job_ads`-count → då kan steg-tracker
  Fas 2 → "Klar" + ev. Fas 2-städning (kvarvarande Fas 2-TDs, §9.6-pressade).
- ADR 0005-observation (dev-test-konto via icke-flag-gejtat /register) →
  auth-fokuserad touch.
- ADR 0040 (smart CV-filter) detaljdesign vid Fas 4-start. TD-84 opportunist.
