---
session: TD-13 FAS 3.5 C4.2 → STOPP I
datum: 2026-05-19
slug: td13-c4-c6
status: STOPP I passerad — STOPP V (Klas-GO) kvarstår
commits:
  - 89545aa feat(security) TD-13 C4.2-C6 (#1c + backfill + crypto-erasure)
---

# TD-13 FAS 3.5 — C4.2/C4.3/C4.4/C5/C6

Levererat non-stop via CTO/architect-kedjan. Full backend-svit grön
(Domain 358 / Application 492 / Migrate 6 / arch 70 / Worker-integ 68 /
Api-integ 344). security-auditor GO (0 Crit/High/GDPR), code-reviewer GO
(0 Block/Major). 4:e user-ägda PII-kolumnen (resume_versions.content)
krypterad. Detaljer per fas i commit `89545aa`.

## Beslut & detourer (det som inte står i commit-msg)

- **C4.2a-gate** GREEN: `MaterializationInterceptionData.GetPropertyValue<T>
  (string)` läser shadow under `AsNoTracking` (Microsoft Learn EF Core 10.0
  verifierad). Avblockerade #1c Form B-read.
- **Architect 2 named adjustments** (inom #1c:s 4 substans-invarianter, ingen
  Klas-STOPP, STOPP V-flaggad): (1) `ContentEnc` shadow UTAN `.IsRequired()`
  (nullable under backfill — C4-review-spec:s `.IsRequired()` var fel mot
  on-disk C4.1); (2) legacy = rå read-only `ContentLegacyJson` jsonb-shadow
  med `PropertySaveBehavior.Ignore` before+after (behållen JSON-VC hade
  återinfört C4.0-RÖD + plaintext-write-back-risk). Tvingade dessutom fram
  `ALTER COLUMN content DROP NOT NULL` (expand-fas, Beslut 5 steg 2 —
  4:e Not 6-precisering, STOPP V-flaggad).
- **CTO Approach A ×2 (probe + unit-test):** #1c eliminerade JSON-VC:n →
  C4.0-probens load-bearing-premiss (prod-modellen applicerar VC:n) föll;
  proben raderad (subsumerad av C4.4 sc.1 mot riktig Postgres + prod-
  interceptorerna). `GetResumeByIdQueryHandlerTests.Handle_WhenResumeExists`
  raderad på samma grund: handlern `ToDetailDto()` dereffererar Content
  ovillkorligt → InMemory (ingen interceptor) NRE:ar; subsumerad av
  `ResumesEndpointsTests.GET_resume_by_id_returns_detail_with_master_version`
  (Api-integ, strikt grön superset). Approach A premiss-falsifierades en gång
  (assertion-only otillräcklig — handlern NRE:ar före) → CTO re-triage →
  Approach C (radera metoden). §7-coverage ej sänkt (flyttad till rätt lager).
- **C5 Npgsql-translate-defekt:** CA1310-analysern tvingade
  `StartsWith(string, StringComparison)` — Npgsql översätter EJ den
  överlagringen → owner-discovery/fitness kastade runtime. Fångad av
  riktig-Postgres-test (InMemory hade gett false positive — ADR 0049
  Mekanik-not 4 i praktiken). Fixad → `EF.Functions.Like` + parameteriserad
  raw-SQL.
- **Sentinel-SSOT** (code-reviewer Finding 1, CTO in-block ej TD): `v1:`
  konsoliderad till `FieldEncryptionSentinel` (VersionPrefix + SqlLikePattern);
  runtime-auktoriteten (`IsEncrypted` regex `^v\d+:`) orörd.
- C5/C6 chassi-paritet HardDeleteAccountsJob/PurgeStaleRawPayloadsJob;
  per-owner fresh DI-scope (cross-user-DEK-isolering); C6 `DeleteDataKeysAsync`
  inom hard-delete-tx (ExecuteDeleteAsync ambient-tx architect-verifierad).
- SQL-bevis: ssyk/region_concept_id STORED generated INTAKTA, raw_payload
  orörd → ingen taxonomi-sök-regression (ADR 0049 Beslut 3 håller).

## Agent-trail (docs/reviews/)
- `2026-05-19-td13-c456-security-audit.md` (GO)
- `2026-05-19-td13-c456-code-review.md` (GO-with-findings, 4 Minor; 2 in-block-
  fixade, 1 dokumenterad design, 1 → CTO in-block SPOT)

## Nästa session
STOPP V = Klas-GO (kan ej autonomt): AWS SSO re-login → tag/deploy-push
v0.2.x-dev → rök-verify (kryptering + taxonomi-regress) → TD-13-arkivering
(§9.7) → ADR Mekanik-not 6-reconciliation (webb-Claude verbatim §9.4) →
FAS 4-startprompt. Cutover-flipp/content-drop = senare egna Klas-STOPP.
