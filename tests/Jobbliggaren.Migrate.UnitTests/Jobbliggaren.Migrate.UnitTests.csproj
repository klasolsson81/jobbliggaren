<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <RootNamespace>Jobbliggaren.Migrate.UnitTests</RootNamespace>
    <TestingPlatformDotnetTestSupport>true</TestingPlatformDotnetTestSupport>
  </PropertyGroup>

  <ItemGroup>
    <Content Include="xunit.runner.json" CopyToOutputDirectory="PreserveNewest" />
    <!-- The deploy stack's compose file. Every DeployCompose* class in this project reads it.
         It landed here for DeployComposeRoleTests, whose connection strings ARE this project's
         subject, and the later compose pins followed the copy rather than adding a second one. -->
    <Content Include="..\..\deploy\docker-compose.yml"
             Link="deploy\docker-compose.yml" CopyToOutputDirectory="PreserveNewest" />
    <!-- #1735 - the dev stack's compose file and the Api's Development settings.
         DeployComposeVolatileRedisTests reads both: the non-persisted Redis exists in both stacks, and
         the dev Api is pointed at it by a committed value rather than by an environment variable. -->
    <Content Include="..\..\docker-compose.yml"
             Link="docker-compose.yml" CopyToOutputDirectory="PreserveNewest" />
    <Content Include="..\..\src\Jobbliggaren.Api\appsettings.Development.json"
             Link="src\Jobbliggaren.Api\appsettings.Development.json" CopyToOutputDirectory="PreserveNewest" />
    <!-- #1232 - PooledConnectionResetTests parses every ConnectionStrings__Postgres the repo
         composes. The deploy stack is one surface; CI's e2e host is the other, and it composes
         its own value rather than reading the compose file. -->
    <Content Include="..\..\.github\workflows\e2e.yml"
             Link=".github\workflows\e2e.yml" CopyToOutputDirectory="PreserveNewest" />
    <!-- #1350 - the Api image. DeployComposeDataProtectionTests binds the compose mount and this
         Dockerfile to the same path: a named volume mounted over a path the image lacks is created
         root-owned, and the container's non-root user cannot then write a key. Boot stays green and
         the failure lands on the first token mint, so nothing else in the repo would catch it. -->
    <Content Include="..\..\src\Jobbliggaren.Api\Dockerfile"
             Link="src\Jobbliggaren.Api\Dockerfile" CopyToOutputDirectory="PreserveNewest" />
  </ItemGroup>

  <!-- #1735 - the compose-file reader (tests/Shared). Linked rather than duplicated: Api.IntegrationTests
       builds a real container from the same file these pins read, and two readers would drift. -->
  <ItemGroup>
    <Compile Include="..\Shared\ComposeFile.cs" Link="Shared\ComposeFile.cs" />
  </ItemGroup>

  <ItemGroup>
    <Using Include="Xunit" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="Shouldly" />
    <PackageReference Include="xunit.v3.mtp-v2" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\..\src\Jobbliggaren.Migrate\Jobbliggaren.Migrate.csproj" />
  </ItemGroup>

</Project>
